# Copy to .env and fill in. .env is gitignored — keep it off the repo.

# The hostname the app will point at. Must already resolve to this VPS,
# or Caddy cannot get a certificate.
DOMAIN=nayla.example.com

# Generate both with: openssl rand -base64 32
POSTGRES_PASSWORD=
AUTHENTICATOR_PASSWORD=
